---
title: Architecture Decisions
type: index
status: active
last_updated: 2026-08-02
summary: Index of all Architecture Decision Records (ADRs) for Parlotype
---

# Architecture Decisions

ADR source files live in `docs/decisions/`. This index provides a quick reference.

| # | Decision | Status | Key Impact |
|---|----------|--------|------------|
| 001 | [[001-global-hotkey-sharphook\|Global Hotkey via SharpHook]] | Accepted | SharpHook for cross-platform global keyboard hooks |
| 002 | [[002-solution-architecture\|Solution Architecture]] | Accepted | Core → Platform → Desktop layered architecture |
| 003 | [[003-audio-pipeline\|Audio Pipeline]] | Accepted | WASAPI → 16kHz mono → VAD → Whisper pipeline |
| 004 | [[004-json-settings-persistence\|JSON Settings Persistence]] | Accepted | `JsonSettingsService` with `SemaphoreSlim` thread safety |
| 005 | [[005-zlogger-structured-logging\|ZLogger Structured Logging]] | Accepted | ZLogger for console + rolling file logging |
| 006 | [[006-text-injection\|Text Injection: Clipboard vs SharpHook]] | Accepted | Clipboard-based injection as default |
| 007 | [[007-whisper-model-selection\|Whisper Model Selection & Download]] | Accepted | Enum-based model selection with HTTP download + progress |
| 008 | [[008-incremental-vad\|Incremental VAD Processing]] | Accepted | Incremental Silero VAD for lower latency |
| 009 | [[009-benchmark-cli-design\|Benchmark CLI Design]] | Accepted | System.CommandLine + Spectre.Console + SQLite |
| 010 | [[010-avalonia-headless-testing\|Avalonia Headless UI Testing]] | Accepted | Avalonia.Headless.XUnit for UI tests |
| 011 | [[011-optimal-stt-pipeline\|Optimal STT Pipeline Settings]] | Accepted | Benchmark-driven optimal Whisper parameters |
| 012 | [[012-cuda-gpu-acceleration\|CUDA GPU Acceleration]] | **Superseded** by 049 | Optional NVIDIA CUDA via Whisper.net.Runtime.Cuda — the `WhisperRuntimeBootstrap` mechanism survives, the CUDA runtime does not |
| 013 | [[013-obsidian-memory-vault\|Obsidian Memory Vault for AI Agents]] | Accepted | Persistent memory substrate for AI agents in `memory/` |
| 014 | [[014-nvidia-environment-detection\|NVIDIA/CUDA Environment Detection]] | Accepted (amended by 049) | First-party provider logs driver/toolkit/runtime at startup; after ADR-049 that log line is its only consumer |
| 015 | [[015-parlotype-desktop-v2-avalonia12\|Parlotype.Desktop — Avalonia 12 Tray-Based UI]] | Accepted | Tray-first frontend on Avalonia 12 (originally Desktop.V2, renamed after V1 sunset) |
| 016 | [[016-avalonia12-developer-tools\|Avalonia 12 Developer Tools (V2)]] | Accepted | DEBUG-only `AvaloniaUI.DiagnosticsSupport` + `avdt` global tool replace retired classic `Avalonia.Diagnostics` in V2 |
| 017 | [[017-whisper-model-hot-swap\|Whisper Model Hot-Swap via UnloadAsync]] | Accepted | `ISpeechRecognizer.UnloadAsync` enables model switching without app restart |
| 018 | [[018-v1-sunset-consolidation\|Sunset V1 — Consolidate on V2]] | Accepted | Removed `Parlotype.Desktop` (V1); implemented WaitTime, punctuation, profanity features in V2 end-to-end |
| 019 | [[019-remove-sub-500ms-silence-threshold\|Remove Sub-500ms Silence Threshold]] | Accepted | Removed Instant/VeryShort/Short from WaitTimeOption; benchmark proved sub-500ms causes 77%+ WER |
| 020 | [[020-sharphook7-simple-global-hook\|Upgrade SharpHook 7 + SimpleGlobalHook]] | Accepted | Switched to SimpleGlobalHook for working event suppression; upgraded SharpHook 6→7.1.1 |
| 021 | [[021-whisper-translation-to-english\|Whisper Translation to English via Settings]] | Accepted | Settings-driven translation toggle; recognizer reinitializes when options change |
| 022 | [[022-vulkan-gpu-acceleration\|Vulkan GPU Acceleration]] | Accepted | `Whisper.net.Runtime.Vulkan` added unconditionally; `RuntimePreference` extended to Auto/Cuda/Vulkan/Cpu with strict no-fallback semantics; new Settings → Runtime section |
| 023 | [[023-audio-level-waveform-visualisation\|Audio-Level Provider & Waveform Visualisation]] | Accepted | `IAudioLevelProvider` interface; `WaveformView` custom control with three visual states (mic icon / breathing bars / animated wave); EMA-smoothed RMS state machine |
| 024 | [[024-gemma4-python-sidecar\|Gemma 4 Python Sidecar]] | Accepted | `Parlotype.Gemma4` project with `Gemma4SpeechRecognizer` implementing `ISpeechRecognizer`; auto-managed Python FastAPI sidecar for benchmark-only Gemma 4 E2B/E4B ASR; bitsandbytes quantization (4-bit default) |
| 025 | [[025-gemma4-llamacpp-desktop\|Gemma 4 via llama.cpp in Desktop]] | Accepted | `SpeechEngine` enum (Whisper/Gemma4); `LlamaCppSpeechRecognizer` spawns llama-server with Vulkan; `DelegatingSpeechRecognizer` routes by setting; English-only, E4B model |
| 026 | [[026-managed-llama-server-install\|Managed llama.cpp Server Installation]] | Accepted | `ILlamaServerCatalog`/`ILlamaServerInstaller`/`ILlamaServerRegistry` in Core; `GitHubLlamaServerCatalog` (HTTP+ETag+1h cache), `JsonLlamaServerRegistry` (manifest.json + `LlamaCppActiveInstall` selector), `LlamaServerInstaller` (staging+SHA256+atomic-rename, cudart companion merge) in Platform; reworked llama settings UI with managed Installed/Available lists and a distinct Manual panel; Phase-1 Windows-only |
| 027 | [[027-llamaserver-namespace-rescope\|LlamaServer Namespace Rescope]] | Accepted | Moved `Parlotype.*.Speech.LlamaServer.*` → `Parlotype.*.LlamaServer.*` (flat). Relocated `LlamaCppServerInfo` to `Parlotype.Platform.LlamaServer`. Anticipates a second consumer (post-processing); pure refactor — no behaviour, schema, or settings change. Renamed `docs/architecture/llamacpp-integration.md` → `llamacpp-subsystem.md`. Flagged `LlamaServerHost` extraction as a follow-up triggered by the first post-processor |
| 028 | [[028-settings-grouped-navigation\|Settings Grouped Navigation]] | Accepted | Settings window reorganised into four categories (Audio / Speech engine / Input / Appearance) with non-selectable group headers; engine-restricted rows hidden for the inactive engine. `SettingsSectionViewModelBase` gains `Category` and `RestrictToEngine`; `SettingsWindowViewModel` projects via `SettingsNavItem`. Old `SpeechSettingsViewModel` split into `SilenceTimeoutSettingsViewModel` (Audio) and `WhisperOutputSettingsViewModel` (SpeechEngine, Whisper-only). Settings keys unchanged |
| 030 | [[030-configurable-gemma4-prompts\|Configurable Gemma 4 Prompts]] | Accepted | `PromptTemplate` record + `IPromptTemplateRegistry` (Core) with `{language}` placeholder/`Render` + `ActivePromptId` key; `JsonPromptTemplateRegistry` (`prompts.json`, non-deletable built-in default merged at read time, corrupt-file quarantine) modelled on `JsonLlamaServerRegistry`; `LlamaCppSpeechRecognizer.TranscribeAsync` resolves active prompt per call (no model reload). New `PromptSettingsViewModel`/`View` (SpeechEngine category, Gemma4-restricted) with inline create/edit/duplicate/delete/select. Shared keyboard-layout source-language setting deferred — only the `{language}` seam built |
| 031 | [[031-github-release-strategy\|GitHub Release Strategy]] | Accepted | Tag-triggered (`v*`) `release.yml`; two self-contained `win-x64` zips per release — **Full** (`EnableCuda=true`, CUDA+Vulkan) and **Lite** (`EnableCuda=false`, Vulkan-only); version derived from tag, hyphenated tags = pre-release; no single-file/trim; windows-latest matrix build + ubuntu release job. Signing & auto-update deferred |
| 029 | [[029-gemma4-model-download-ui\|Gemma 4 Model Download UI]] | Accepted | `Gemma4ModelInfo` is a 5-entry catalog (E2B Q8_0/BF16, E4B Q4_K_M/Q8_0/BF16) keyed by `ModelId`, with `Gemma4Variant`+`Gemma4Quant` enums + `SelectedGemma4Model` settings key (E2B has no Q4_K_M upstream). New `Gemma4ModelSettingsViewModel`/`View` (SpeechEngine category, Gemma4-restricted) with per-model Download/Delete via reused `ModelDownloadDialog` (now with a completion **Close** state + cumulative GGUF+mmproj progress) + new `Gemma4ModelDownloadDialogService`. `Gemma4ModelDownloadService` parameterised by model + `DeleteModelAsync` (streams scoped before `File.Move`). `LlamaCppSpeechRecognizer` resolves selected model via `GetById`. Chose own C# downloader over llama-server `-hf` (progress UX). No SHA/token/migration |
| 031 | [[031-benchmark-warmup-pass\|Benchmark Warm-up Pass]] | Accepted | Every `benchmark run` now performs one throwaway `TranscribeAsync` on the first sample between `InitializeAsync` and the timed loop to prime CUDA/OS page cache/first-inference paths. New nullable `BenchmarkSummary.WarmupTimeMs` + `warmup_ms REAL` column in `SqliteResultIndex` (idempotent `PRAGMA table_info` migration). Reporters add Warm-up row; `ComparisonResult.WarmupDelta` populated only when both runs have data. GC baselines captured after warm-up; warm-up exceptions fail loud. No config/CLI flag — always on |
| 032 | [[032-online-speech-providers-positioning\|Online Speech Providers — Brand & Positioning]] | Accepted | Single-app product with **local-by-default, cloud-by-choice** positioning. Tagline preserved. No rename, no separate SKU. 5 brand commitments (local stays default; opt-in; transparency; BYOK; tagline preserved). Replaces "privacy-first" wording across docs/vault. Implementation (provider selection, key storage, settings UI, fallback behaviour, etc.) explicitly deferred to follow-up ADRs |
| 033 | [[033-translation-model-capability\|Translation Capability per Whisper Model]] | Accepted | `WhisperModelInfo.SupportsTranslation` (Core) is the single source of truth — `false` for `*En` models + `LargeV3Turbo`. `AudioPipelineService` gates the effective flag (`intent && SupportsTranslation`) so translation never reaches an incompatible model; the saved `TranslateToEnglish` preference is preserved (not overwritten) and restored on switching back. UI signals twice: muted "no translation" hint in the model list + disabled toggle (`WhisperOutputSettingsViewModel.CanTranslate`) with note, wired via `SettingsWindowViewModel` reacting to `SelectedModel` changes. Fulfils the deferred enforcement from ADR-021 |
| 034 | [[034-source-target-language-selection\|Source & Target Language Selection]] | Accepted | Core `LanguageCatalog` (curated `WhisperLanguages` ~99 + `CultureInfo`-derived `AllLanguages` fallback), `LanguageInfo`, `LanguageCapabilities`/`SpeechEngineCapabilities.For`, `RecentLanguages` MRU helper; new keys `SelectedSourceLanguage`/`SelectedTargetLanguage`/`RecentLanguages`. `AudioPipelineService` reads source language into `WhisperOptions.Language` (was hard-coded `"auto"`); `LlamaCppSpeechRecognizer.BuildPromptTextAsync` renders `{language}` + appends a target-language translation instruction (single LLM call). New engine-aware `LanguageSelectionSettingsViewModel`/`View` (source picker both engines; target picker only for arbitrary-translation engines = Gemma 4) with recent-5 pinned on top, wired via `SettingsWindowViewModel.UpdateForEngine`. Whisper translation stays English-only via existing toggle (ADR-033). Pipeline 2 (Whisper→LLM) + keyboard-layout source detection deferred |
| 035 | [[035-language-settings-ux-redesign\|Language Settings UX Redesign]] | Superseded by 036 | Unified Language page: `[Source] → [Target]` row with the arrow as translation toggle, inline shared `LanguagePickerView`/`LanguagePickerViewModel` (search + MRU rows), one picker visible at a time. New `SettingsKeys.TranslationEnabled`; per-role `RecentSourceLanguages`/`RecentTargetLanguages` MRU. Whisper-output `Translate to English` toggle removed — `AudioPipelineService` derives `TranslateToEnglish = TranslationEnabled && target == "en"` (plus ADR-033 capability gate); `LlamaCppSpeechRecognizer` gates the translation prompt on `TranslationEnabled`. `LanguageSettingsMigrator` (idempotent, called from pipeline + Gemma prompt builder + Language VM) one-shot migrates legacy `TranslateToEnglish` and shared `RecentLanguages`. `SpeechEngineCapabilities.For(Whisper).FixedTranslationTargets` now `[English]` so the unified picker renders Whisper's target uniformly. "Translation paused" hint moved to Language page |
| 036 | [[036-language-ux-rebuild\|Language UX Rebuild]] | Accepted | Supersedes 035 UX (data model/migration stand). Core: `LanguageCatalog.KeyboardLayoutCode` ("keyboard") sentinel + `IsKeyboardLayout`, `TranslationForm` enum (None/Toggle/Full) derived on `LanguageCapabilities`, `IKeyboardLayoutService`/`KeyboardLayoutInfo`, pure `SourceLanguageResolver` (keyboard→detected, auto fallback, optional supported-list check). Platform: `Win32KeyboardLayoutService` (P/Invoke `GetKeyboardLayout` on the foreground window's thread; HKL low-word LANGID → `CultureInfo`) + `NoOpKeyboardLayoutService` non-Windows; `AudioPipelineService` + `LlamaCppSpeechRecognizer` resolve the sentinel. Desktop: shared `LanguageRelationshipViewModel` (DI singleton; state machine, resting-target restore, per-role MRU, connector on/off/locked + glyph, summary, switch-fallback toasts) consumed by the Language page **and** the Transcribe window; floating popover pickers (`Border.popoverChrome`, rich rows, Recent/All groups, >8 search rule); model-driven target forms (ToggleSwitch / picker / disabled+amber); Transcribe quick-picker strip + 268px flyout (target control + read-only source row → Settings) |
| 037 | [[037-gemma4-source-target-prompts\|Source & Target Languages in Gemma 4 Prompts]] | Accepted | Retires `{language}` for `{speech_lang}`/`{text_lang}` (no alias; `Substitute` helper, `AutoDetectedLanguageName`). Built-in default `PromptTemplate` gains optional `TranslationText`/`AutoDetectText` (3 bodies: transcription/translation/auto-detect); custom prompts stay single-body (null bodies, no `prompts.json` migration). `LlamaCppSpeechRecognizer.BuildPromptTextAsync` selects the body via a source/target matrix — built-in picks the dedicated body, custom appends the translation sentence; `{speech_lang}`→"the detected language" when auto. **Keeps** the `TranslationEnabled` toggle for Gemma 4 (parity with Whisper). Amends ADR-030 + ADR-034. Editor stays single-body; intro/tip copy updated. **Amendment 2026-08-01:** `{text_lang}` substituted on every path — target name while translating, speech language otherwise (custom prompts were leaking the raw token on all paths but the built-in translation body); Prompts page gains a collapsible "How prompts work" panel (`IsHelpExpanded`); all three built-in bodies (and the new-prompt template) gain `Use punctuation.` — deliberate divergence from Google's template, pinned by a test |
| 038 | [[038-speech-model-prewarm-and-loading-state\|Speech-Model Prewarm & On-Button Loading State]] | Accepted | Adds `RecordingState.Loading` (Core) + `WaveformView` rotating-arc spinner + `TranscribeViewModel.IsLoading`/`Button.loading` blue style so the first record press no longer looks like a dead button. Spinner is **deferred** (`LoadingSpinnerDelay` default 200ms races `StartAsync` vs `Task.Delay`) so a hot model doesn't flash it. Whisper load (`WhisperFactory.FromPath`/`Build`) wrapped in `Task.Run` so it doesn't freeze the UI-thread spinner. New default-interface `IAudioPipeline.PrewarmAsync` (no-op default — mocks unchanged); `AudioPipelineService` extracts `EnsureModelInitializedAsync` guarded by `SemaphoreSlim _initLock` (idempotent via recognizer's option-matched `InitializeAsync`). Prewarm is **opt-in, off by default** (`SettingsKeys.PrewarmModelOnStartup`): `App` gates the background `TranscribeViewModel.PrewarmAsync` on it; the toggle lives in the always-visible **Engine** settings section (`SpeechEngineSettingsViewModel.PreloadModelOnStartupEnabled`) and only persists (effect next launch). Spinner remains the cold-start safety net |
| 039 | [[039-ptt-stop-waits-for-inflight-start\|Push-to-Talk Stop Waits for In-Flight Start]] | Accepted | Fixes first-use PTT never stopping: `TranscribeViewModel` tracks the in-flight start as `Task? _startTask`; `StopRecordingAsync` awaits it before the `IsRecording` guard so a key release during the cold model load stops the recording instead of being silently dropped; `StartRecordingAsync` is reentrancy-guarded (pipeline started once). Fix lives in the ViewModel so hotkey, toggle button, and `RelationshipChanged` auto-stop are all covered. Regression tests in `TranscribeViewModelTests` |
| 040 | [[040-frameless-compact-transcribe-window\|Frameless Compact Transcribe Window]] | Accepted | Transcribe window becomes a 172×118 frameless widget (design C2, Windows Voice Typing style): `WindowDecorations="None"` + transparent window + rounded `RootChrome` border; drag confined to a top grip strip (`BeginMoveDrag`; on Windows the modal move loop returns at drop, position saved then); ✕/`Esc` hide to tray; status text lives only in the root tooltip. Position persistence uses a new `IWindowStateService`/`JsonWindowStateService` (own `window-state.json`, shares a `JsonFileStore` base with `JsonSettingsService`) — kept separate from `ISettingsService` so frequent drag saves never touch user settings — storing one `WindowPosition` struct under `WindowStateKeys.TranscribeWindowPosition`. `RestorePositionAsync` (called by `WindowManager` pre-show) applies the saved position only when still on a connected screen, else CenterScreen. Headless coverage in `TranscribeWindowChromeTests` + `JsonFileStoreTests`; drag itself needs manual verification |
| 041 | [[041-parakeet-v3-sherpa-onnx\|Parakeet TDT 0.6B v3 via sherpa-onnx]] | Accepted | Third engine `SpeechEngine.Parakeet`: in-process sherpa-onnx `OfflineRecognizer` (`nemo_transducer`, INT8, CPU-only — no prebuilt GPU NuGet), ~670 MB 4-file HF download to `models/<modelId>/` via `ParakeetModelDownloadService`; 25 European languages always auto-detected (no language-forcing param), transcribe-only ⇒ `TranslationForm.None`; `ParakeetModelInfo` catalog + `LanguageCatalog.ParakeetLanguages` in Core; Desktop engine card + Parakeet-restricted model section; Benchmark `parakeet` config section (smoke: WER 5.6 %, RTF 0.072 CPU). "Whisper remains default" superseded by ADR-042. Amended 2026-07-08: second catalog entry `parakeet-tdt-0.6b-v3-fp32` (~2.6 GB, encoder graph + ONNX external-data `encoder.weights`; smoke WER 1.9 % at ~2× decode time / ~3× RAM) selectable in Settings; INT8 stays default |
| 042 | [[042-parakeet-default-language-ux\|Parakeet Default & Capability-Driven Language UI]] | Accepted | Parakeet becomes the default engine (all unset-setting fallbacks; explicit `SpeechEngine=Whisper` untouched; engine list reordered Parakeet-first; first-use model download goes through the shared dialog with progress + Cancel via new Core `IParakeetModelProvider` — Platform headless impl overridden by Desktop dialog service, decline ⇒ `OperationCanceledException`). Language UI hides entirely for engines with no language choice: `LanguageCapabilities.SupportsSourceSelection` (false for Parakeet) + derived `HasLanguageChoices`; `SettingsSectionViewModelBase.IsVisibleFor(engine)` virtual (Language page overrides); Transcribe strip hidden + window compacts 118→88 px; `ApplyEngine` skips spec-§8 fallbacks (nothing persisted) so language prefs survive a Parakeet round trip |
| 043 | [[043-cloud-speech-providers-v1\|Cloud Speech Providers v1 — OpenAI-Compatible + xAI Grok]] | Accepted | First two opt-in cloud engines per ADR-032: `SpeechEngine.OpenAiCompatible` (`OpenAiCompatibleSpeechRecognizer` → `POST {base}/audio/transcriptions`; base-URL swap covers OpenAI/Groq/any compatible host) + `SpeechEngine.XaiGrok` (`XaiGrokSpeechRecognizer` → `POST {base}/stt`, custom field names). Batch multipart WAV upload only (no streaming; `WavEncoder` extracted from LlamaCpp recognizer). BYOK API keys in new Core `ISecretStore` → Platform `DpapiSecretStore` (`secrets.json`, DPAPI CurrentUser on Windows / base64+warning elsewhere; new dep `System.Security.Cryptography.ProtectedData`). Transcribe-only capabilities; shared `CloudSpeechLanguageResolver`/`CloudSpeechHttpError` helpers; post-processing stays centralized in `AudioPipelineService`. Desktop: engine cards with "audio is sent to provider" wording, `CloudProviderSettingsViewModel` section (write-only key entry), persistent Cloud badge on `TranscribeWindow` (ADR-032 commitment #3). Amended 2026-07-10: missing-key failures throw typed `CloudProviderNotConfiguredException` (Core); `TranscribeViewModel` catches it and shows a fire-and-forget `ConfirmationDialog` ("Open settings" → deep link `SettingsSection.CloudProviders`) via new `IUserDialogService`/`UserDialogService`. Amended 2026-07-10 (provider errors): `CloudSpeechHttpError` parses OpenAI error envelopes + classifies into `CloudSpeechErrorKind` (quota/rate-limit/5xx/key), throws typed `CloudSpeechTranscriptionException`; `IAudioPipeline.TranscriptionFailed` event surfaces it; `TranscribeViewModel` shows a message dialog (rejected key → "Open settings") with single-flight guard. Amended 2026-07-10 (language UX): cloud engines are auto-detect-only like Parakeet — `SupportsSourceSelection: false` ⇒ language UI hides + widget compacts; recognizers send no `language` part (`CloudSpeechLanguageResolver` deleted) |

| 044 | [[044-microbenchmark-project\|Micro-benchmark Project (BenchmarkDotNet)]] | Accepted | `src/Parlotype.MicroBenchmarks` console project: BenchmarkDotNet 0.14 + `MemoryDiagnoser`/`ShortRunJob` for audio hot-path allocation evidence; distinct from `Parlotype.Benchmark` (quality); not in `dotnet test`; frozen legacy copies (e.g. `LegacyWavEncoder`) keep before/after in one run; artifacts git-ignored, curated tables live in the plan folder |
| 045 | [[045-audio-pipeline-allocation-threading\|Audio Pipeline Allocation & Threading Rework]] | Accepted | Capture callback rents from `ArrayPool<float>` (was ~1.5 MB/s LOH garbage from `new float[BytesRecorded]` — a byte count used as a float count); `AudioDataEventArgs.Buffer` valid only during the event; `WavEncoder` exact-size rewrite 6–8× faster at ½ alloc (byte-identical); Parakeet zero-copy via `MemoryMarshal.TryGetArray`; buffer pre-size + span `AddRange` (must pair — unsized `AddRange` allocates *more*); pipeline restructured into 3 channel-joined single-threaded stages — VAD off the capture thread (no more overflow-drop risk), transcription event-driven (50 ms polling removed), stop = channel-completion drain |
| 046 | [[046-security-hardening-batch\|Security Hardening Batch (2026-07 Audit)]] | Accepted | Audit report `docs/security/2026-07-11-security-audit.md`. Transcripts never logged (standing convention; file sink capped at Information); SHA-256 verification on all model downloads (fail-closed mismatch ⇒ `ModelIntegrityException`, fail-open missing digest; `WhisperModelInfo.Sha256` replaces unused SHA-1, Parakeet/Gemma catalogs gain digests); `CloudBaseUrlValidator` HTTPS-or-loopback at recognizer init + settings hint; clipboard exclusion formats on injected text (no Win+V history / cloud sync); llama-server `ArgumentList`; `AtomicFileWriter` for settings/secrets. Deferred: sidecar `--api-key` (S5), non-Windows keychain (S8) |

| 047 | [[047-multi-binding-dictation-hotkeys\|Multi-Binding Dictation Hotkeys]] | Accepted | Users configure a *list* of gestures, not one chord. `DictationHotkey` = `HotkeyGesture` (Chord/HoldModifier/DoubleTapModifier + `ModifierSide`) + per-binding `ActivationMode`; defaults Hold Right Ctrl (PTT) / Double-tap Ctrl (toggle) / Ctrl+Alt+Space, retiring Ctrl+Shift+Space (IDE parameter hints). Recognition moves to Core (`HotkeyGestureMatcher` + tap/hold trackers, pure and timestamp-driven); `SharpHookHotkeyService` becomes an adapter. Breaking `IGlobalHotkeyService` reshape to semantic Start/Stop/Cancel events + `SetDictationActive`. Escape cancels via a real discard path (`CancelRecordingAsync` detaches `TranscriptionAvailable` before stopping). Bare modifiers are **never** suppressed; hold-start defers 250 ms when a double-tap shares the key. Two-tier conflict validation (Blocking/Warning) incl. the user's own bindings; `HotkeyBindings` string list + one-shot legacy migration that preserves a user's custom chord alone |
| 048 | [[048-whisper-runtime-latch-and-factory-lifetime\|Whisper Runtime Latch Detection & Factory Lifetime]] | Accepted | Fixes the "loaded 'Cuda' instead of 'Vulkan'" failure plus its ~3 GB-per-attempt native leak. Whisper.net resolves its runtime once per process, so a preference changed mid-session can only apply after restart: new Core `IWhisperRuntimeStatus` (→ Platform `WhisperRuntimeStatus` over `WhisperRuntimeBootstrap`) exposes the latch, `WhisperRuntimeBootstrap.IsSatisfiedBy` is the single matcher (`Auto` accepts anything, `Cpu` accepts `Cpu`+`CpuNoAvx`), and `AssertRuntimeStillSelectable` throws **before the model download** with `RuntimeUnavailableException.RequiresRestart` — including for `Cpu`, which previously ran silently on the latched GPU. Leak cause: `WhisperFactory` holds the native context in a bare `IntPtr` with **no finalizer**, and the old code assigned `_factory` before the post-load assertion while `UnloadAsync` early-returned on `!IsReady`; now `CreateVerifiedFactory` disposes on every failure branch, `_factory` is published only after the processor builds, and `UnloadAsync` releases whatever exists. UI: Settings → Runtime shows a live "Restart required" panel; `TranscribeViewModel` says "Restart Parlotype to use the X runtime". Verified: 746 MB growth over 5 failed inits (`base.en`) → 0 MB |
| 049 | [[049-drop-whisper-cuda-runtime\|Drop the Whisper CUDA Runtime]] | Accepted | CUDA removed entirely — `Whisper.net.Runtime.Cuda` + the `EnableCuda` MSBuild flag deleted, `RuntimePreference` is now `Auto/Vulkan/Cpu` (`Auto` = Vulkan → CPU), `WhisperSpeechRecognizer` drops `INvidiaEnvironmentProvider`, and Settings loses the NVIDIA-driver / CUDA-toolkit guidance panels. Rationale: Whisper is no longer the default engine (Parakeet is, CPU-only), the "Full" build was never self-sufficient (no bundled cudart/cublas ⇒ user still needed the ~3 GB toolkit), and benchmarks show CUDA only 8–26 % faster by RTF with identical WER on Small/Medium and *worse* WER on LargeV3Turbo. Cost: NVIDIA users lose that speed and ~800 MiB moves from VRAM back to host RAM on the larger models. `INvidiaEnvironmentProvider` (ADR-014) survives for startup diagnostics; Gemma 4 keeps CUDA via downloadable `llama-server` builds. Supersedes ADR-012, amends ADR-031 (release is now a single `Parlotype-<version>-win-x64.zip`, no Full/Lite matrix). Measured published output: 731 MB, cut to 338 MB by ADR-050 |

| 050 | [[050-drop-onnx-runtime-gpu-providers\|Drop the Unused ONNX Runtime GPU Providers]] | Accepted | New solution-wide `Directory.Build.targets` filters `onnxruntime_providers_cuda` / `_tensorrt` out of `ReferenceCopyLocalPaths` (build) and `ResolvedFileToPublish` (publish). The 391 MB CUDA provider came from `SileroVad 1.3.0` → `Microsoft.ML.OnnxRuntime.Gpu` (not sherpa-onnx, as ADR-049 first guessed) and can never load: VAD uses default CPU session options, Parakeet pins `Provider = "cpu"`, and the providers target ORT 1.18.1 while sherpa-onnx’s own newer `onnxruntime.dll` wins the output folder. Proven inert by running the Parakeet smoke benchmark with and without the DLL present — identical WER/CER/RAM. `onnxruntime.dll`, `onnxruntime_providers_shared.dll` and the managed assembly are kept. Published `win-x64` output 731 MB → **338 MB**; the dependency graph is deliberately untouched. Next size target: the 100 MB of Skia/HarfBuzz native PDBs |
| 051 | [[051-publish-only-target-rid-runtimes\|Publish Only the Target RID's Whisper Native Libraries]] | Accepted | A `-r win-x64` publish still shipped Whisper natives for Linux, macOS, win-x86 and win-arm64 (~60 MB, 45 of it `libggml-vulkan-whisper.so`). Cause: `Whisper.net.Runtime`/`.Vulkan` declare their natives as plain `<None>` content with a hard-coded `TargetPath`, not `runtimes/<rid>/native/` assets, so SDK RID filtering never sees them — and their own gating keys on TargetFramework, where `net10.0` (no platform suffix) fires the Windows *and* macOS blocks while Linux is unconditional. `RemoveForeignRuntimeAssetsFromPublish` in `Directory.Build.targets` keys on the immediate parent directory (the RID in both the `runtimes/<rid>/` and `runtimes/vulkan/<rid>/` layouts) and matches against an explicit RID list minus the current one; publish-only, so `bin/` and tests are untouched. 338 MB → **278 MB** (731 MB → 278 MB across ADR-049/050/051). Verified by publishing the benchmark and hiding each runtime folder in turn — both remain functional |
| 052 | [[052-drop-native-pdbs-from-publish\|Drop Native PDBs from Release Publish Output]] | Accepted | `libSkiaSharp.pdb` (80 MB) and `libHarfBuzzSharp.pdb` (20 MB) — flagged as the next size target by ADR-050 — come from Avalonia's Skia backend via `SkiaSharp.NativeAssets.Win32`/`HarfBuzzSharp.NativeAssets.Win32`. New `RemoveNativePdbsFromPublish` target in `Directory.Build.targets` filters `ResolvedFileToPublish` on `%(Extension) == '.pdb'` and `%(AssetType) == 'native'` — the same tag NuGet stamps on `runtimes/<rid>/native/` assets, which cleanly excludes Parlotype's own managed PDBs (no `AssetType` at all) without a filename list. `Condition="'$(Configuration)' == 'Release'"` so `-c Debug` publishes keep every symbol; no RID gating needed since `AssetType == 'native'` only exists for the resolved publish RID. Published `win-x64` Release output 278 MB → **180 MB** (731 MB → 180 MB across ADR-049/050/051/052). Verified by launching `Parlotype.Desktop.exe` from the trimmed output (starts clean, hotkeys + Vulkan/GPU detection fire) and confirming a `-c Debug` publish still contains both native PDBs |

| 053 | [[053-velopack-packaging-and-auto-update\|Velopack Packaging & Auto-Update]] | Accepted | Velopack 1.2.0 (`vpk` CLI + `Velopack` package, lock-stepped) replaces the unzip-and-run zip: per-user `Setup.exe` into `%LOCALAPPDATA%\Parlotype` with no UAC, Start Menu shortcuts, and delta updates (measured 0.1.0→0.1.1 delta **96 KB** vs 77 MB full). **packId `Parlotype` is permanent.** The sharp edge: Velopack deletes the *entire* `%LOCALAPPDATA%\{packId}` folder on uninstall **and on a Setup.exe re-run** (velopack#120), and Windows paths are case-insensitive — so the old data root `%LOCALAPPDATA%\parlotype` *was* the pack folder. Shipping that would have erased multi-GB model caches and DPAPI API keys. Data root therefore moved to `%LOCALAPPDATA%\parlotype-data` behind a new Core `IAppPaths`/`AppPaths` (BCL-only, lives in Core because `ParakeetModelInfo`/`Gemma4ModelInfo` need it and Core can't reference Platform); `AppPathsTests` fails if any path lands inside the pack folder. **No automatic migration** — documented manual folder rename. `VelopackApp.Build().Run()` is the first statement in `Program.Main` (vpk verifies this statically at pack time); `OnFirstRun` pre-creates dirs; Windows-only `OnBeforeUninstallFastCallback` keeps data by default and deletes `DataDirectory` only when `UninstallRemovesUserData` is set — hooks can't show UI, so consent is captured **in advance** from Settings → Application → Data and the hook just executes it (parses settings.json directly, since it runs before DI; anything ambiguous reads as false). Same page offers "Delete downloaded models…" which unloads the recognizer + stops the sidecar first (Windows locks loaded model files). Windows-only — macOS/Linux have no uninstall hooks. New Core `IUpdateService` → Platform `VelopackUpdateService` over `GithubSource`: startup + 6-hourly check, background download, **apply on exit** (downloading only *stages* — Velopack installs nothing without an explicit `ApplyOnExit()`/`ApplyAndRestartAsync()`, so a plain quit-and-relaunch would never install it); `NotInstalledException`/`IsInstalled` make dev and portable builds a silent no-op. `Updates.CheckAutomatically` defaults **on** — anonymous unauthenticated GET of the public releases API, no machine id/usage data, disclosed in README and Settings → Application → Updates with a one-click opt-out. `<SelfContained>` pinned in the csproj (conditioned on a RID to dodge NETSDK1031) so a lost CI flag can't silently ship framework-dependent; `--framework` deliberately unused. One channel per platform; CI matrix keyed on rid/runner/channel so macOS/Linux are additive rows. `vpk download` before `vpk pack` is mandatory or every release is a full download |

| 054 | [[054-curated-release-notes\|Curated Release Notes from CHANGELOG.md]] | Accepted | Releases shipped with **no body at all** — `vpk upload github` sets only `--releaseName` and assets, and GitHub then renders the tagged commit's message, which for a squash-merged PR is the whole engineering commit body (see [[github-release-empty-body-fallback]]). New repo-root `CHANGELOG.md` (Keep a Changelog, user-facing wording, ≤3 Highlights, `⚠️ Action required` when it applies, internal churn in a collapsed *Under the hood* block, backfilled 0.1.0–0.4.0) is the single source of truth. `release.yml` gains **`Extract release notes`** — placed before `Restore` so a missing section fails in seconds, not after a 20-minute publish; requires a non-empty `## [X.Y.Z]` section on a tag push, only checks the `## [Unreleased]` heading on a dry run — and **`Set release notes`** running `gh release edit --notes-file` after `vpk upload` (chosen over an unconfirmable `vpk --releaseNotes` flag: `gh` is preinstalled, `contents: write` already granted, and overwriting after upload is idempotent under `--merge true`). Drafting is a **local** skill, `.claude/skills/release-notes/SKILL.md` — reads `git log` bodies over the tag range plus every referenced ADR (whose `## Context` is already user-facing prose), classifies user-facing vs internal, opens a PR and **stops**; never tags, never touches a published release. Rejected: `claude-code-action` in CI (needs an `ANTHROPIC_API_KEY` in a repo that needs no secrets, and publishes unreviewed wording) and GitHub's `generate_release_notes` (a PR-title list — same contributor register, just shorter). Extractor uses `substr`/`index`, not regex: escaping `\[` and `\.` survives neither YAML nor every awk. Consequence: **tagging now has a precondition** — an unmerged changelog section fails the build before anything uploads, and the tag must be deleted and re-pushed at a commit containing the notes |

| 055 | [[055-single-instance-guard\|One Instance Per Session, With Activation]] | Accepted | Nothing stopped several Parlotypes running at once, and each one installs its own `TaskPoolGlobalHook`: a single hotkey press reached every instance, several recordings started, and whichever finished first injected text — `SuppressEvent` decides the ordering, so the outcome was undefined. Tray-first startup (no window, ADR-040) is what produces the second launch in the first place. New Desktop `SingleInstanceGuard` takes the named mutex `Local\Parlotype.SingleInstance` in `Program.Main` — **after** `VelopackApp.Run()` (install/update/uninstall hooks re-invoke the same exe and must not be turned away) and **before** Avalonia (a process about to exit shows nothing); the loser calls `SignalPrimary()` and returns 0. A named mutex, not a process-list scan (the requested approach): the kernel decides ownership atomically, so two launches at login can't both win, and ownership dies with the process, so a crash leaves no stale state. **The second launch activates the first** rather than exiting silently — the primary owns a named auto-reset event watched on a background thread, wired by `App` to `IWindowManager.ShowTranscribe()`; the event is created when the mutex is won, not when the listener starts, so a signal during the multi-second startup is queued rather than dropped (auto-reset events stay signalled until waited on) and the secondary needs no retry loop. Session-scoped (`Local\`) because hotkeys/audio/tray are per-session — a second signed-in user gets their own instance. Fail-open throughout: `AbandonedMutexException` (killed instance) counts as acquiring, any other failure logs to `velopack.log` and reports primary, since not starting is worse than starting twice. Activation is Windows-only (named `EventWaitHandle`s throw on Unix; the mutex itself is portable). Not a DI service — it predates `BuildServiceProvider`, so it lives on `Program.SingleInstance` like `Program.TextInjectionMode`, and adds no Core interface. Consequence: a dev build no longer runs alongside an installed one — quit the installed app first; `Acquire(name)` takes an override so tests never touch the real lock |
| 056 | [[056-first-run-onboarding-wizard\|First-Run Onboarding Wizard with Live UI Highlighting]] | Accepted | 8-step tour in a frameless Topmost `OnboardingWindow` that opens the real windows via `IWindowManager` and highlights live controls: new attached property `OnboardingTarget.Id` markers + `OnboardingHighlightService` attaching a pulsing `OnboardingHighlight` adorner via `AdornerLayer.SetAdorner` (layout tracking free; pending ids retried on `LayoutUpdated`; missing/invisible targets silently skipped). Declarative `OnboardingStep` records from `OnboardingStepFactory.Build(bindings)` — recording step names the user's actual `DictationHotkey.DisplayString`s, honouring an emptied list (ADR-047). Deep links: `SettingsSection.Engine`/`EngineModel` (resolves to the active engine's model page, cloud → Engine fallback)/`Help`. Once-only via `SettingsKeys.OnboardingCompleted` (string-bool, stamped **before** show; deliberately not Velopack-`OnFirstRun`-gated so updaters + dev builds see it once). New always-visible **Settings → Help** section (`HelpSettingsViewModel`, Application category): live hotkey reference + "Open the tour" via `IOnboardingService`/`OnboardingService`. First localization layer: `Resources/Strings.resx` + hand-written public `Strings` accessor; all tour/Help copy externalized, markup binds VM properties only. Desktop-only — Core gains just the settings key |
| 057 | [[057-cancel-dictation-on-command-shortcuts\|Cancel Dictation on Command Shortcuts, and Make Cancel a Real Abort]] | Accepted | Fixes the gap ADR-047 recorded and dismissed: a slow `Right Ctrl+C` past the 300 ms `HoldAbortGraceMs` left a recording that stopped normally, and ADR-047's "it captures silence, so nothing is injected" only holds when the user isn't speaking — mid-sentence it transcribed and typed into the user's editor. The grace window's premise ("users type while dictating") cannot apply to Ctrl or Alt, since nothing typed under a command modifier reaches the target app as text. `ModifierHoldTracker.IsCommandModifierHold` therefore drops the timing term for **Ctrl/Alt** holds — any keystroke aborts, whenever it lands; Shift (composes text) and Meta (not a shipped gesture) keep the window. Scope is holds only, deliberately not "any Ctrl/Alt keystroke while dictating": in toggle mode the modifier is genuinely free and `Ctrl+S` should save, not destroy the recording. Suppression unchanged (ADR-047 §5), so the user gets the shortcut *and* loses the recording. Second half: cancel was never a real discard — detaching `TranscriptionAvailable` then calling `StopAsync` still drained, flushed, and ran the recognizer with a hardcoded `CancellationToken.None` under a 30 s budget. New `IAudioPipeline.CancelAsync` (no default impl — delegating to `StopAsync` would silently transcribe) behind a shared `AudioPipelineService.ShutdownAsync(discard, ct)`: `_discarding` + a `_transcribeCts` set before the raw writer completes, `FlushBuffer` skips the final VAD pass, `TranscribeLoopAsync` drains without recognizing, `OperationCanceledException` no longer raises `TranscriptionFailed` (a cancel is not an error dialog), drain budget 30 s → 5 s. Both `TranscribeViewModel` discard paths switch over. Caveat: sherpa-onnx can't interrupt a decode already in native code, so one short utterance's tail may still run — discarded either way, guaranteed by a session-scoped `cancellationToken` recheck in `TranscribeLoopAsync` right before publishing (a code-review catch: without it, a decode outliving the drain timeout could publish into a session that had since restarted). Same review also caught that nothing serialized `StartAsync` against `ShutdownAsync`, so two overlapping shutdown calls could both pass the `IsRunning` gate or have a stale call's cleanup null out a session a concurrent start had already begun — fixed with a new `_lifecycleLock` (same pattern as the existing `_initLock`) |

| 058 | [[058-installer-code-signing\|Installer Code Signing with Azure Artifact Signing]] | Accepted | Closes the consequence ADR-053 left open ("builds are unsigned, SmartScreen warns"). **Azure Artifact Signing** (ex-Trusted Signing, $9.99/mo) over an OV cert (reputation accrues per-cert over download volume a low-volume project may never reach) or EV (hardware token ⇒ self-hosted runner). Signing runs **inside `vpk`**, never over `Releases/` afterwards — `Update.exe` is signed *before* embedding in the `.nupkg` and `Setup.exe` *after* its payload is appended, so `azure/artifact-signing-action`/`signtool` post-passes append a cert table past Setup's payload and can break the installer. **Everything in the package is signed, not just `Setup.exe`**: `Update.exe` runs on every update and `Parlotype.exe` daily, and Smart App Control + WDAC/AppLocker evaluate *every loaded binary* — unsigned whisper.cpp/sherpa-onnx/ONNX Runtime/Vulkan natives are exactly what gets blocked. 5,000 signatures/mo vs a few hundred PE files, so budget isn't a constraint (`--signExclude`/`VPK_SIGN_EXCLUDE` if it becomes one). Switched on by **`VPK_AZURE_TRUSTED_SIGN_FILE`**, not a flag — the `Pack` step is byte-for-byte identical signed and unsigned, so credentialed and uncredentialed paths can't drift and ADR-053's "nothing else in this workflow changes" holds literally; dry runs and fork PRs (whose `id-token: write` is downgraded to read) keep packing unsigned, which is what keeps the packaging dry run — the only check for a lost `--self-contained` — credential-free. Auth is **OIDC, no client secret**: Entra app registration with federated subject `repo:mdemin729/parlotype:environment:release` + *Artifact Signing Certificate Profile Signer* on the account; the dlib's `DefaultAzureCredential` falls through to the Azure CLI credential `azure/login` leaves behind. The `release` environment must carry **no** deployment branch/tag policy or the dry runs block. `setup-dotnet` now installs **8.0.x alongside 10.0.x** — the dlib vpk bundles is built against .NET 8 and won't load otherwise; nothing is built with 8. New `Verify signatures` step asserts on *output* (a signing failure inside vpk doesn't reliably fail the pack): `Setup.exe` **and** the main exe extracted from the portable zip, both `Valid` **and timestamped** — Artifact Signing certs live **72 hours**, so an untimestamped signature starts failing three days after release. Gotcha: `SIGN_ENDPOINT` must name the same region as the account *and* the profile; a mismatch surfaces as a bare 403 from `SignerSign()` mentioning nothing about regions |

| 059 | [[059-launch-at-sign-in\|Launch at Sign-In, On by Default]] | Accepted | Parlotype is hotkey-driven and starts tray-only (ADR-056), so a machine restart silently killed the product: hotkey pressed, nothing happens, no clue why. Registers via the per-user **`HKCU\…\CurrentVersion\Run`** key — no elevation (matches ADR-053's per-user install), no COM, and crucially it's what **Task Manager → Startup apps** lists, i.e. where users go to stop something launching; a Startup-folder `.lnk` needs `IShellLink` P/Invoke and hides from that list, Task Scheduler sells delayed/elevated starts this app doesn't want. `Microsoft.Win32.Registry` resolves on plain `net10.0` with **no PackageReference** (only `[SupportedOSPlatform("windows")]` for CA1416 under warnings-as-errors) — verified by compiling. Registered path is the Velopack **stub** `%LOCALAPPDATA%\Parlotype\Parlotype.exe`, never `current\Parlotype.exe` (replaced wholesale on update); resolved from `VelopackLocator.Current` and **null for non-installed builds** (`dotnet run`, IDE, unpacked zip) so a dev machine never picks up an entry pointing at a path about to move — mirrors `UpdateState.NotInstalled`, with the toggle greyed out and explained. `SettingsKeys.LaunchAtLogin` is string-bool where **absent/unparsable means on** (same shape as `UpdatesCheckAutomatically`), so **existing installs adopt it on their first launch after updating**, not just fresh ones — rejected the `OnFirstRun`-gated fresh-installs-only variant as splitting one policy into two paths and leaving the current user base without the thing that makes the hotkey work. Not shipped silently: the tour's existing "Parlotype lives in the tray" step (ADR-056, `Strings.resx`, so it translates) now states it starts with Windows and where to switch it off. **`GetState()` returns a 4-state enum, not a bool**, because Windows gets a vote: Explorer records a Task Manager veto in `…\Explorer\StartupApproved\Run` as a 12-byte blob (low bit of byte 0 = disabled) **separately from the `Run` value**, so a bool-valued setting reads "on" while nothing launches — see [[windows-run-key-startup-approval]]. Parlotype detects and explains that state but **never writes** `StartupApproved` (undocumented, Explorer-cached, and it would override a deliberate user decision). A registration pointing elsewhere reports `Disabled`, so a stale entry from a moved install is rewritten by the ordinary reconcile path with no special case. `LaunchAtLoginCoordinator` (Platform) owns the default-on rule for both callers — `App` at startup (fire-and-forget, off the UI thread) and `StartupSettingsViewModel` on page load — writing only when preference and observed state differ, and doing nothing at all when blocked. Nothing throws: a registry that won't cooperate is a lost convenience, logged and reported `Disabled`. macOS/Linux get `NoOpLaunchAtLoginService` (`SMAppService` and XDG autostart are unshipped work) |
| 060 | [[060-hold-scoped-push-to-talk\|Hold-Scoped Push-to-Talk, and Per-Engine Utterance Ceilings]] | Accepted | `AudioPipelineService` ended utterances on a **silence timeout** even in push-to-talk, where the key release already says when the user finished — a guess layered on a fact, and a wrong guess cuts mid-sentence, corrupting the words either side (a cut through "this evening" produced **"V7"**), punctuating a fragment, and injecting it before the sentence is done. Very Long (3 s) is the longest setting and does not cover pauses past 3 s, so affected users had nothing left to turn. New **`PipelineMode.SingleUtterance`**: silence never ends the utterance, only the explicit stop does; the existing stop-time `FlushBuffer` already did the right thing. **VAD is kept, not bypassed** — the original request was to "ignore the VAD", but its *segmenting* role is what hurts while its *filtering* role (trim silence, drop dead air, `InterSegmentSilenceMs` joins) is pure benefit. Mode is **derived from the gesture, not a setting**: `HotkeyMatchResult.HoldScoped` (hold tracker, or a chord bound `ActivationMode.PushToTalk`) travels `DictationStartEventArgs` → `HotkeyCoordinator` → `TranscribeViewModel.StartRecordingAsync(bool)` → `StartAsync`, since nothing downstream can re-derive it; the widget's record button stays Batch. Ceilings are **per-engine and mostly about correctness** (`SpeechEngineLimits`): Parakeet **60 s**, Whisper **300 s** (latency only), unmeasured engines 60 s. Parakeet's is not a cost guard — past it sherpa-onnx *silently drops text* (100 % of words at 60 s → 60 % at 120 s → 48 % at 300 s, CER tracking WER, the signature of missing rather than misheard text) and above **400 s** it throws `SEHException` out of native ONNX Runtime where a 5000-frame positional-encoding buffer (80 ms/frame = 400.0 s) runs out; today's 30 s cap is the only reason the app never reaches it. Whisper showed no knee at all (99 % retention, flat WER at 600 s) because whisper.cpp chunks internally at 30 s. The ceiling measures **speech** (`AccumulatedSpeechSamples`), not elapsed hold time — the extractor discards the pauses and the benchmarks measured decode input, so checking the raw buffer would split a pause-heavy hold for nothing; a 10-min raw backstop bounds memory for a missed key-up. Engine is read *after* the recognizer initializes (`SpeechRecognizerFactory` re-reads it independently and the engine can change mid-start; reading last makes any drift conservative, not dangerous). Over the ceiling `FlushAtSpeechBoundary` flushes completed segments and retains the final possibly-mid-word run, so the cut lands in a pause — and an unbroken run is cut *at* the ceiling with the overrun retained, never discarded (VAD lags the buffer by up to 500 ms, so that tail is live speech) — also fixing a latent bug where batch mode's overflow path raw-dumped `_sampleBuffer.ToArray()` with no VAD extraction (visible as flush-3000 scoring 16.2 % WER / 89 % retention at 300 s against flush-1000's 4.8 % / 100 %). Silence timeout setting stays for toggle mode; its "Applies to all speech engines" copy was false and now says push-to-talk ignores it. Cost: latency moves to the end of the hold — nothing appears until release |

| 061 | [[061-translation-paused-state\|Translation Paused Is a First-Class UI State]] | Accepted | ADR-033's capability data and pipeline gate survived; its **UI half did not** — the ADR-036 language rebuild carried the model-capability flag over as one boolean bound to a single accent line on the Settings page, so with Medium (English) or Large v3 Turbo the Transcribe strip still read `English → English` in accent blue, the flyout showed no note (the amber one was bound to `IsNoneForm` only), and the Language page contradicted itself — connector `On`, switch on, summary claiming an English output. The exact ADR-033 symptom ("enabled translation, nothing translated, no visible cause") was back by another route. New **`ConnectorState.Paused`** alongside On/Off/Locked, derived from `IsTranslationPaused` (= toggle form + translation on + `!WhisperModelSupportsTranslation`, replacing `ShowTranslationPausedNote`). Kept distinct from `Locked` because cause and cure differ: locked = the *engine* can't translate (change engine), paused = *this model* can't (change model). Rejected the tidier fix of making `SpeechEngineCapabilities.For` model-aware so the engine collapses to `TranslationForm.None` — `ApplyEngine` force-disables translation on that form and would destroy the preserved intent that is ADR-033's whole point. Every derived string now agrees with the pipeline: `SummaryText` says "types Russian (translation paused)", the glyph is `=` not `→`, `TranscribeViewModel.TargetShort` mirrors the source. The toggle stays **on and operable** (superseding ADR-033's disable-the-toggle point — a disabled control can't express intent for later); the target card gains a "Paused" sub-line beside the live switch. The VM holds the active `WhisperModel` so `TranslationPausedNote`/`ConnectorTooltip` name it, and both the page banner and the Transcribe flyout carry a **"Choose a model that translates"** button → `SettingsSection.EngineModel`, reached through a new `SettingsSectionViewModelBase.NavigationRequested` event (sections ask the shell, never a sibling). `SetWhisperModel` toasts when a switch *newly* pauses a live translation; startup reconciliation stays silent. The model list's grey "no translation" became a bordered amber badge with a tooltip. **Palette stays per-view, on evidence:** hoisting the two amber brushes to `Application.Resources` was tried and reverted — the headless tests host controls under `TestApp`, so app-scoped resources resolve to nothing there and every amber state rendered as plain text with all assertions still green (caught only by reading pixels out of the screenshot report); the same run showed `DynamicResource` in a `Styles` setter not reaching the view-local dictionary either, so the paused connector drew fully transparent. Hence literal `#C68A28` in the class styles (mirroring `.on`'s literal `#378ADD`), `{DynamicResource}` only for direct properties, and `WarnPaletteResourceTests` pinning all three surfaces in both variants. See [[avalonia-resource-scope-in-headless-tests]]. Risk: `SetWhisperModel` is wired only from `SettingsWindowViewModel`'s model-section subscription plus one startup call, so any future writer of `SelectedWhisperModel` bypassing that section leaves the paused state stale until relaunch |

## Template

New ADRs use `docs/decisions/_template.md`. Use the next sequential number — list `docs/decisions/` and pick `(highest existing number) + 1`. Do **not** rely on a hard-coded "next" hint here, it goes stale.
